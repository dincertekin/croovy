[tool.poetry]
name = "croovy-backend"
version = "1.0.0"
description = ""
authors = ["Dinçer Tekin <dincertekin@protonmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.13"
redis = "^5.2.1"
flask = "^3.1.0"
flask-cors = "^5.0.0"

[tool.poetry.scripts]
start = "src.app:create_app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
